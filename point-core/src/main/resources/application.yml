spring:
  mvc:
    path match:
      matching-strategy: ant_path_matcher

  application:
    name: point-core

  #  db
  datasource:
    url: jdbc:mysql://localhost:23306/local_db
    username: root
    password: 102030
